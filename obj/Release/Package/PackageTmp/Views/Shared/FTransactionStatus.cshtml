@model FMDSS.Models.CitizenService.PermissionServices.FixedLandUsage
@{
    ViewBag.Title = "PaymentSuccessfull";
}
 
<style>
    table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
    }

    th, td {
        padding: 5px;
        text-align: left;
    }

    table#tblScart tr:nth-child(even) {
        background-color: #eee;
    }

    table#tblScart tr:nth-child(odd) {
        background-color: #fff;
    }

    table th {
        background-color: dimgrey;
        color: white;
    }

    table tr:nth-child(even) {
        background-color: #eee;
    }

    table tr:nth-child(odd) {
        background-color: #fff;
    }

    table th {
        background-color: dimgrey;
        color: white;
    }

    table tr:nth-child(even) {
        background-color: #eee;
    }

    table tr:nth-child(odd) {
        background-color: #fff;
    }

    table th {
        background-color: dimgrey;
        color: white;
    }

    table tr:nth-child(even) {
        background-color: #eee;
    }

    table tr:nth-child(odd) {
        background-color: #fff;
    }

    table th {
        background-color: dimgrey;
        color: white;
    }
</style>
<script type="text/javascript" language="javascript">

    $(document).ready(function () {

        function disableBack() { window.history.forward() }

        window.onload = disableBack();
        window.onpageshow = function (evt) {
            if (evt.persisted) disableBack()
        }

    });

 
        function printDiv(divID) {
            //Get the HTML of div
            var divElements = document.getElementById(divID).innerHTML;
            //Get the HTML of whole page
            var oldPage = document.body.innerHTML;

            //Reset the page's HTML with div's HTML only
            document.body.innerHTML = 
              "<html><head><title>Transaction Status</title></head><body>" +
              divElements + "</body>";

            //Print Page
            window.print();

            //Restore orignal HTML
            document.body.innerHTML = oldPage;

          
        }
 
</script>

<div id="page-wrapper-inn">

    <div class="col-lg-12 pad">
        <h1 class="page-header">Transaction Status</h1>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <br /><br /><br /><br />
                <div id="Print_table">
                    <table style="text-align: right; border-width: 1px; height: 350px; width: 70%; margin: auto;" bordercolor="blue" cellpadding="2" cellspacing="5">

                        <tr>
                            <td>Transaction No:</td>
                            <td>@Model.TransactionId </td>
                        </tr>
                        <tr>
                            <td>Transaction Amount:</td>
                            <td>@Model.Final_Amount</td>
                        </tr>
                        <tr>
                            <td>Name:</td>
                            <td>Gaurav</td>
                        </tr>

                        <tr>
                            <td>Payment Status:</td>
                            <td>@Model.PayStatus</td>
                        </tr>


                    </table>
                </div>
                <br />
                @using (Html.BeginForm("", "", FormMethod.Post, new { id = "pay" }))
                {
                    <div style="text-align:center;" class="form-horizontal" role="form">
                        <button id="btnPrint" type="submit" name="Command" class="btn btn-success" value=" print" onclick="javascript: printDiv('Print_table'); return false;">Print</button>
                        <a href='@Url.Action("dashboard", "dashboard")'>
                            Home
                        </a>
                            @*<button class="btn btn-warning">                               

                                <i class="fa fa-circle-o-notch fa-fw"></i> Home
                            </button>*@
                       
                        
                    </div>
                }
            </div>
        </div>
    </div>
</div>








