@model FMDSS.Models.WaterFireModelData
@{
    ViewBag.Title = "Vendor";
    Layout = Convert.ToString(HttpContext.Current.Session["CurrentLayout"].ToString()); // "~/Views/Shared/_SuperAdmin.cshtml";
}


<script type="text/javascript" src="~/js/jquery.min.js"></script>

<script type="text/javascript">
        var RootUrl = '@Url.Content("~/")';

        $(document).ready(function () {

            $('#dataTables-example').DataTable({
                responsive: true
            });

        });


</script>

@*@using (Html.BeginForm("ImportData", "Master", FormMethod.Post, new { id = "myForm", enctype = "multipart/form-data" }))
    {*@
<div id="page-wrapper-inn">
    <div class="col-lg-12 pad">
        <h1 class="page-header">Vendor List</h1>
    </div>
    <div class="row">
        <div class="col-lg-4 pull-right" id="place">
        </div>
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="dataTable_wrapper">
                        <div id="tbl" class="table-responsive">
                            <table class="table table-striped table-bordered table-hover table-responsive" id="dataTables-example">

                                <thead>
                                    <tr>
                                        <th style="width:8px">SNo.</th>
                                        <th>Reg.No</th>
                                        <th>Vendor Name</th>
                                        <th>SSO ID</th>
                                        <th>Vendor Type</th>
                                        <th>Representative Name</th>
                                        <th>Circle/Division/Range</th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @foreach (var item in Model.List)
                                    {
                                        <tr>
                                            <td style="width:10px">
                                                @Html.DisplayFor(model => item.SNO)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(model => item.RegNumber)

                                            </td>

                                            <td>
                                                @Html.DisplayFor(model => item.VendorName)

                                            </td>
                                            <td>
                                                @Html.DisplayFor(model => item.SSOID)

                                            </td>
                                            <td>
                                                @Html.DisplayFor(model => item.VendorType)

                                            </td>
                                            <td>
                                                @Html.DisplayFor(model => item.RepresentativeName)

                                            </td>
                                            <td>
                                                @item.CircleName / @item.DivisionName / @item.RangeName

                                            </td>


                                        </tr>

                                    }
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>

            <div class="modal fade" id="myModal" role="dialog">
                <div class="modal-dialog modal-lg" id="modelPlace">
                    <!-- Modal content-->


                </div>
            </div>
        </div>
    </div>

</div>
@*}*@
