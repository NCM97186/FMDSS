@{
    ViewBag.Title = "PaymentResponse";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model FMDSS.Models.PGResponse

<h2>PaymentResponse</h2>
<style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 70%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }

    table.imagetable {
        font-family: verdana,arial,sans-serif;
        font-size: 11px;
        color: #333333;
        border-width: 1px;
        border-color: #999999;
        border-collapse: collapse;
    }

        table.imagetable th {
            background: #b5cfd2 url('cell-blue.jpg');
            border-width: 1px;
            padding: 8px;
            border-style: solid;
            border-color: #999999;
        }

        table.imagetable td {
            background: #ccc;
            border-width: 1px;
            padding: 8px;
            border-style: solid;
            border-color: #999999;
        }
</style>
<div id="page-wrapper-inn">
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Payment Response
                </div>
            </div>
            <div class="panel-body">
                <div class="col-lg-12 un-bold">
                    <table class="table table-striped table-bordered table-hover">
                        <tr>
                            <td>
                                @Html.ActionLink("Download Ticket", "GetTicketForPrint", "BookArcheologyTicket", new { PRNNumber = @Model.PRN }, new { @target = "_blank" })
                            </td>
                        </tr>
                    </table>

                    <table class="table table-striped table-bordered table-hover">

                        <tr><td>Request Id :</td><td>@Model.PRN </td></tr>
                        <tr><td>Response Message :</td><td> @Model.RESPONSEMESSAGE </td></tr>
                        <tr><td>PaidAmount :</td><td> @Model.PAIDAMOUNT </td></tr>
                        <tr><td>Status :</td><td> @Model.STATUS </td></tr>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script type="text/javascript">

        function callfunction(PRN) {



            $.ajax({
                type: 'GET',
                contentType: 'application/json; charset=utf-8',
                //data: JSON.stringify(values),
                url: '@Url.Action("GetTicketForPrint", "BookArcheologyTicket")?PRNNumber=' + PRN,
                success: function (data) {

                    //var response = JSON.parse(data);
                    //for (var i = 0; i < response.length; i++)
                    //    {
                    //          addCheckbox(response[i].AreaName , response[i].PK_Id);
                    //    }
                }
            });
            alert(PRN);
        }

    </script>
